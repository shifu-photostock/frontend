'use strict'
let initialState = {
  logged: false,
};

const userReducer = (state=initialState, action) => {
  switch(action.type) {
  case '': 
    break;
  }
  return state;
};

export default userReducer;
